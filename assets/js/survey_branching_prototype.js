document.addEventListener('click',e=>{const b=e.target.closest('[data-expand],[data-skip]');if(!b)return;const m=b.closest('.module');if(!m)return;if(b.hasAttribute('data-expand')){m.classList.remove('skipped');m.classList.toggle('open');b.textContent=m.classList.contains('open')?'閉じる −':'もっと答える ＋';}else{m.classList.remove('open');m.classList.toggle('skipped');b.textContent=m.classList.contains('skipped')?'スキップ解除':'今回はスキップ';const ex=m.querySelector('[data-expand]');if(ex)ex.textContent='もっと答える ＋';}});